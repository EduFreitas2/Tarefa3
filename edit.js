function is_even(first_value) {
  if(first_value % 2 == 0)
  {
    return true;
  }
else{
  return false;
}
  
 }
   function is_even(second_value) {
    if(second_value % 2 == 0)
    {
      return true;
    }
  else{
    return false;
  }
    
   }
  function add(first_value, second_value) {
  soma = first_value + second_value
  return soma;
   }
   function sub(first_value, second_value) {
    return first_value - second_value;
     }
     function mul(first_value, second_value) {
      return first_value * second_value;
       }
       function div(first_value, second_value) {
        return first_value / second_value;
         }
        
  
  

   